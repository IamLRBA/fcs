/**
 * Inset vertical rails on horizontal scrollers (scrollport box-shadow in globals.css).
 * Do not use Tailwind arbitrary rgba — it often fails to compile. Company marquee is separate.
 */
export const SLIDER_SYNC_EDGE_LINE_CLASS = 'slider-sync-edge-inset-rails'
