/**
 * Vertical rail bars beside horizontal scrollers — colours match company marquee
 * (see `Companies.tsx` inline border colors). Styled in globals.css.
 */
export const SLIDER_SYNC_EDGE_LINE_CLASS = 'slider-sync-edge-rail-bar'
