import { SkeletonProductCategory } from '@/components/ui/Skeleton'

/**
 * Category page loading – mirrors hero only (image + title + quote + pills).
 */
export default function CategoryLoading() {
  return <SkeletonProductCategory />
}
