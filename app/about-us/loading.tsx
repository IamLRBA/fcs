import { SkeletonAboutFirst } from '@/components/ui/Skeleton'

/**
 * About page loading – mirrors first section (title + subtitle + banner).
 */
export default function AboutLoading() {
  return <SkeletonAboutFirst />
}
